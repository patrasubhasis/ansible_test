---
- hosts: all
  become: yes
  tasks:
  - name: update repo
    ansible.builtin.apt:
      update_cache: yes
  - name: upgrade packages
    ansible.builtin.apt:
      upgrade: yes
  - name: install apache2
    ansible.builtin.apt:
      name: apache2
      state: latest
